<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="StocksMapper">
  <insert id="insertTest" parameterType="List">
	  	insert into MARKET_SHOES(
	  		MS_ID,
	  		MS_URL,
	  		MS_NAME,
	  		MS_PRICE,
	  		MS_USER_NAME,
	  		MS_REG_DATE 
		) values
  	<foreach collection="stocks" item="shoes">
    	 (
    	 	#{shoes.id},
    	 	#{shoes.url},
    	 	#{shoes.name},
    	 	#{shoes.price},
    	 	#{shoes.size},
    	 	#{shoes.status},
    	 	#{shoes.user},
    	 	#{shoes.regDate}
    	 )
  	</foreach>
  </insert>
</mapper>